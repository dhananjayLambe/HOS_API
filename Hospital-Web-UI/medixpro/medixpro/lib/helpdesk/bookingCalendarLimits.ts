/** Align with Django `settings.MAX_BOOKING_DAYS` (default 30). */
export const HELPBOOK_MAX_BOOKING_DAYS = 30;

/** Align with Django `settings.BOOKING_SLOT_LEAD_BUFFER_MINUTES` (default 5). */
export const SLOT_LEAD_BUFFER_MINUTES = 5;
