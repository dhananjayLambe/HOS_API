# Incident Narrative

`NarrativeBuilder` generates **deterministic** template-based narratives.

Example:

> Booking completed successfully. Routing failed: Price service timeout. WhatsAppDelivery retried 2 time(s) before succeeding. Total workflow duration was 28 min.

No LLM calls in M5.7. Future AI copilots consume `IncidentReport` as input.
