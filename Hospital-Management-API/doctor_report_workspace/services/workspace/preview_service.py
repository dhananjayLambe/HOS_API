"""PreviewService — preview URL / payload orchestration."""


class PreviewService:
    """Builds preview payloads (signed URLs or stream metadata)."""

    def build_preview(self, *, report_id, artifact_id, scope):
        raise NotImplementedError("Milestone 0 scaffold — not implemented yet.")
