{
  "version": 4.1,

  "meta": {
    "rules": {
      "no_hard_required": true,
      "importance_over_required": true,
      "structured_dependencies": false,
      "free_text_allowed": true,
      "validation_driven": true
    },

    "validation_defaults": {
      "severity": "error",
      "skip_if_hidden": true
    },

    "ui_behavior": {
      "show_inline_errors": true,
      "show_summary": false
    },

    "empty_values": [null, "", []],

    "guidance": "Findings are objective observations. They should never block workflow.",
    "updated_at": "2026-04-18T10:00:00Z"
  },

  "pallor": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "pallor_severity",
        "key": "severity",
        "label": "Severity",
        "type": "select",
        "options": ["Mild", "Moderate", "Severe"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "pallor_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "edema": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "edema_site",
        "key": "site",
        "label": "Site",
        "type": "select",
        "options": ["Feet", "Ankles", "Generalized"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "edema_pitting",
        "key": "pitting",
        "label": "Pitting?",
        "type": "radio",
        "options": ["Yes", "No"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "edema_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "pedal_edema": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "pedal_edema_severity",
        "key": "severity",
        "label": "Severity",
        "type": "select",
        "options": ["Mild", "Moderate", "Severe"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "pedal_edema_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "icterus": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "icterus_present",
        "key": "present",
        "label": "Icterus Present?",
        "type": "radio",
        "options": ["Yes", "No"],
        "required": false,
        "default": { "value": "No", "apply_on": ["init"] },
        "validation": { "required": false }
      },
      {
        "id": "icterus_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "lymphadenopathy": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "lymph_region",
        "key": "region",
        "label": "Region",
        "type": "select",
        "options": ["Cervical", "Axillary", "Inguinal", "Generalized"],
        "is_multi": true,
        "required": false,
        "validation": { "required": false, "min_items": 0, "max_items": 4 }
      },
      {
        "id": "lymph_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "respiratory_distress": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "resp_distress_severity",
        "key": "severity",
        "label": "Severity",
        "type": "select",
        "options": ["Mild", "Moderate", "Severe"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "resp_distress_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "wheeze": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "wheeze_side",
        "key": "side",
        "label": "Side",
        "type": "select",
        "options": ["Bilateral", "Right", "Left"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "wheeze_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "abdominal_tenderness": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "abd_tender_location",
        "key": "location",
        "label": "Location",
        "type": "select",
        "options": ["Upper", "Lower", "Generalized"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "abd_tender_severity",
        "key": "severity",
        "label": "Severity",
        "type": "select",
        "options": ["Mild", "Moderate", "Severe"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "abd_tender_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "joint_swelling": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "joint_swelling_joint",
        "key": "joint",
        "label": "Joint",
        "type": "select",
        "options": ["Knee", "Shoulder", "Elbow", "Wrist", "Ankle"],
        "is_multi": true,
        "required": false,
        "validation": { "required": false, "min_items": 0, "max_items": 5 }
      },
      {
        "id": "joint_swelling_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  },

  "heart_murmur": {
    "version": 1,
    "display": { "hide_if_empty": true },
    "fields": [
      {
        "id": "heart_murmur_present",
        "key": "present",
        "label": "Murmur Present?",
        "type": "radio",
        "options": ["Yes", "No"],
        "required": false,
        "validation": { "required": false }
      },
      {
        "id": "heart_murmur_additional_notes",
        "key": "additional_notes",
        "label": "Additional notes",
        "type": "textarea",
        "required": false,
        "validation": { "type": "text", "required": false, "max_length": 500 }
      }
    ]
  }
}